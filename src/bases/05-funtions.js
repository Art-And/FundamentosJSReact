const saludar = (nombre) => (
  `Hola, ${nombre}`
  );

console.log(saludar('Andres'));


const getUserActive = (nam) => (
  {
    uid: 'FASDFDAS',
    username: nam,
  }
);

const userActive = getUserActive('Archivaldo')
console.log(userActive);
